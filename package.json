{
  "name": "minex",
  "version": "0.0.1",
  "private": true,
  "description": "Minex — 领域无关的插件宿主内核（微内核架构）",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @minex/kernel && npm run build -w minex-demo-plugin && npm run build -w @minex/cli",
    "typecheck": "npm run typecheck -w @minex/kernel && npm run typecheck -w minex-demo-plugin && npm run typecheck -w @minex/cli",
    "test": "vitest run",
    "cli": "npm run build -w @minex/cli && node packages/cli/dist/cli.js",
    "plugins:sync": "npm run build -w minex-demo-plugin && node scripts/sync-plugins.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
